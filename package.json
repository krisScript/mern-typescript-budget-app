{
  "name": "mern-typescript-budget-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "scripts": {
    "test": "lerna run test",
    "install-stack": "lerna run install",
    "start-dev": "lerna run start --stream"
  }
}
