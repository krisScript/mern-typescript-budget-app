{
  "name": "mern-typescript-budget-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^4.1.0"
  },
  "scripts": {
    "install-server": "cd server && yarn install",
    "install-client": "cd client && yarn install",
    "install-stack": "yarn install && yarn install-server && yarn install-client",
    "start": "concurrently \"cd server &&  yarn start\" \"cd client && yarn start \""
  }
}
